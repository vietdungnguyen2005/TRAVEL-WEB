{
    "name": "gateway",
    "version": "0.1.0",
    "private": true,
    "description": "LEGACY/EXPERIMENTAL gateway. Prefer services/api-gateway (canonical) used by docker-compose.",
    "scripts": {
        "dev": "node -e \"console.log('apps/gateway is legacy/experimental and has no src/. Use services/api-gateway.');\"",
        "build": "node -e \"console.log('apps/gateway build is disabled (legacy). Use services/api-gateway.');\"",
        "start": "node -e \"console.log('apps/gateway start is disabled (legacy). Use services/api-gateway.');\""
    },
    "dependencies": {
        "express": "^4.18.2",
        "zod": "^4.2.1"
    },
    "devDependencies": {
        "ts-node-dev": "^2.0.0",
        "typescript": "^5.0.0"
    }
}